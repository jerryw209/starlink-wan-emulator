syntax = "proto3";

package SpaceX.API.Device;

message PublicKey {
	optional string key = 1;
	repeated Capability capabilities = 2;
}

enum Capability {
	READ = 0;
	WRITE = 1;
	DEBUG = 2;
	ADMIN = 3;
	SETUP = 4;
	SET_SKU = 5;
	REFRESH = 6;
	READ_PRIVATE = 7;
}
